replicaCount: 2

image:
  repository: 337405469571.dkr.ecr.ap-southeast-2.amazonaws.com/devops-capstone-app
  tag: "d0974624cb778bfe5a1186b47634b01500b64b8a"
  pullPolicy: IfNotPresent

imagePullSecrets:
  - name: aws-ecr-secret

service:
  type: ClusterIP
  port: 8080
  targetPort: 8080

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 250m
    memory: 256Mi

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 10
  targetCPUUtilizationPercentage: 50

alertmanager:
  config:
    global:
      slack_api_url: 'https://hooks.slack.com/services/WEBHOOK/URL'
    route:
      group_by: ['alertname', 'namespace']
      group_wait: 30s
      group_interval: 5m
      repeat_interval: 1h
      receiver: 'slack-notifications'
    receivers:
    - name: 'slack-notifications'
      slack_configs:
      - channel: '#alerts'
        send_resolved: true
        title: '[{{ .Status | toUpper }}] {{ .GroupLabels.alertname }}'
        text: >-
          {{ range .Alerts }}
            *Description:* {{ .Annotations.description }}
            *Severity:* {{ .Labels.severity }}
          {{ end }}
